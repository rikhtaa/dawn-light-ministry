# Environment variables — Dawn of Light Ministry
#
# No environment variables are required yet; the site currently has no
# features that need secrets. This file documents variables anticipated
# for upcoming features so they are declared here ahead of implementation
# (see CLAUDE.md section 20).
#
# Copy this file to .env.local and fill in real values only once the
# corresponding feature is implemented. Never commit .env.local.

# --- Prayer / Contact form email delivery (not yet implemented) ---
# EMAIL_PROVIDER_API_KEY=
# EMAIL_FROM=
# EMAIL_TO=pastornayyer@gmail.com
